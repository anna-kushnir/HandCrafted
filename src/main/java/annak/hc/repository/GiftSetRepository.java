package annak.hc.repository;

import annak.hc.entity.GiftSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GiftSetRepository extends JpaRepository<GiftSet, Long> {
}
